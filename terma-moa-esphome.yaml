substitutions:
  # Replace with your Terma MOA Blue MAC address - see on android settings or the nrf connect app
  radiator_mac: !secret xiao_esp32_c6__radiator_mac

esphome:
  name: xiao-esp32-c6
  friendly_name: XIAO ESP32 C6
  min_version: 2026.4.0
  name_add_mac_suffix: false

esp32:
  board: seeed_xiao_esp32c6
  framework:
    type: esp-idf

external_components:
  - source:
      type: local
      path: components

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: !secret xiao_esp32_c6__encryption_key

# Allow Over-The-Air updates
ota:
  - platform: esphome
    password: !secret xiao_esp32_c6__ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

captive_portal:

# Enable BLE with keyboard capability for PIN entry
esp32_ble:
  io_capability: keyboard_only

# Enable the Bluetooth tracker
esp32_ble_tracker:
  scan_parameters:
    active: true
    interval: 320ms
    window: 30ms

# Persistent connections to these devices need encryption - otherwise the device can only be controlled if connected in pair mode.
# The first bootup the moa device must be in pair mode and any connections afterwards can be normal.
ble_client:
  - mac_address: ${radiator_mac}
    id: terma_moa_ble_client
    auto_connect: true
    on_connect:
      then:
        # Encryption is now requested automatically by the terma_moa component
        # the instant the connection opens - no lambda needed here.
        - logger.log: "BLE client connected - encryption requested automatically"
        # Give the encryption/bonding handshake time to complete before we
        # treat the connection as usable.
        - delay: 2s
        - lambda: |-
            id(is_connected) = true;
        - logger.log: "BLE connection ready"
    on_passkey_request:
      then:
        - logger.log: "Passkey requested by radiator - sending 123456"
        - ble_client.passkey_reply:
            id: terma_moa_ble_client
            passkey: 123456
    on_passkey_notification:
      then:
        - logger.log:
            format: "Passkey notification received: %d"
            args: ["passkey"]
    on_disconnect:
      then:
        - logger.log: "BLE client disconnected!"
        - lambda: |-
            id(is_connected) = false;

# Terma MOA Blue helper component - automatically requests link encryption on
# connect so the radiator will serve its (encrypted) characteristics.
terma_moa:
  ble_client_id: terma_moa_ble_client

# Globals to store state
globals:
  - id: power_state
    type: bool
    restore_value: yes
    initial_value: "false"
  - id: is_connected
    type: bool
    restore_value: no
    initial_value: "false"
  - id: operating_mode
    type: int
    restore_value: yes
    initial_value: "0"

# ============================================
# BLE Sensors from Radiator
# ============================================
sensor:
  # Room temperature sensor (reads from b1 characteristic) - radiator's built-in sensor.  This sensor is not accurate.
  - platform: ble_client
    type: characteristic
    ble_client_id: terma_moa_ble_client
    id: room_temp_raw
    name: "Radiator Room Sensor"
    internal: false
    service_uuid: "d97352b0-d19e-11e2-9e96-0800200c9a66"
    characteristic_uuid: "d97352b1-d19e-11e2-9e96-0800200c9a66"
    update_interval: 30s
    notify: false
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    device_class: temperature
    icon: "mdi:thermometer"
    lambda: |-
      ESP_LOGD("terma", "b1 room raw (%d bytes): %s", (int) x.size(), format_hex_pretty(x).c_str());
      if (x.size() >= 4) {
        // Little-endian uint16, value / 10 = °C (verified Terma Blue Line protocol).
        float current_room = (x[0] + x[1] * 256.0f) / 10.0f;
        float target_room = (x[2] + x[3] * 256.0f) / 10.0f;
        ESP_LOGD("terma", "Room - Current: %.1f°C, Target: %.1f°C", current_room, target_room);
        return current_room;
      }
      return NAN;

  # Heater temperature sensor (reads from b2 characteristic)
  - platform: ble_client
    type: characteristic
    ble_client_id: terma_moa_ble_client
    id: heater_temp_raw
    name: "Radiator Element Temperature"
    internal: false
    service_uuid: "d97352b0-d19e-11e2-9e96-0800200c9a66"
    characteristic_uuid: "d97352b2-d19e-11e2-9e96-0800200c9a66"
    update_interval: 30s
    notify: false
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    device_class: temperature
    icon: "mdi:radiator"
    lambda: |-
      ESP_LOGD("terma", "b2 element raw (%d bytes): %s", (int) x.size(), format_hex_pretty(x).c_str());
      if (x.size() >= 4) {
        // Little-endian uint16, value / 10 = °C (verified Terma Blue Line protocol).
        float current_heater = (x[0] + x[1] * 256.0f) / 10.0f;
        float target_heater = (x[2] + x[3] * 256.0f) / 10.0f;
        ESP_LOGD("terma", "Heater - Current: %.1f°C, Target: %.1f°C", current_heater, target_heater);
        // Sync the radiator's own element target (e.g. changed with the physical
        // buttons) back into the Rail Target number. publish_state only updates the
        // shown value - it does NOT trigger set_action, so there is no write-back loop.
        if (target_heater >= 30.0f && target_heater <= 60.0f) {
          id(rail_target).publish_state(target_heater);
        }
        return current_heater;
      }
      return NAN;

  # Operating mode sensor (reads from b3 characteristic)
  - platform: ble_client
    type: characteristic
    ble_client_id: terma_moa_ble_client
    id: mode_raw
    name: "Operating Mode Raw"
    internal: true
    service_uuid: "d97352b0-d19e-11e2-9e96-0800200c9a66"
    characteristic_uuid: "d97352b3-d19e-11e2-9e96-0800200c9a66"
    update_interval: 30s
    notify: false
    lambda: |-
      // Log ALL bytes. The mode characteristic is 4 bytes wide; a bounded-timer
      // countdown may live in bytes 1-3 (undocumented). Capture this in each mode
      // (off / continuous / timed / schedule) to find the timer discriminator.
      ESP_LOGD("terma", "b3 mode raw (%d bytes): %s", (int) x.size(), format_hex_pretty(x).c_str());
      if (x.size() >= 1) {
        int mode = x[0];
        id(operating_mode) = mode;
        // Off is 0x00 (physical off) or 0x20 (off via app on newer firmware).
        bool heating = (mode != 0x00 && mode != 0x20);
        id(power_state) = heating;
        ESP_LOGD("terma", "Operating mode: 0x%02X (%d) heating=%d", mode, mode, heating);
        return (float) mode;
      }
      return NAN;

  # Other sensors
  - platform: wifi_signal
    name: "WiFi Signal"
    update_interval: 60s

  - platform: uptime
    name: "Uptime"
    update_interval: 60s

# Binary Sensors
binary_sensor:
  - platform: status
    name: "Connection Status"

  - platform: ble_presence
    mac_address: ${radiator_mac}
    name: "Radiator BLE Presence"

  - platform: template
    name: "Radiator BLE Connected"
    id: ble_connected_sensor
    lambda: |-
      return id(is_connected);

  - platform: template
    name: "Heating Active"
    id: heating_active
    lambda: |-
      return id(power_state);
    device_class: heat

  - platform: template
    name: "Heating Without Timer"
    id: heating_without_timer
    device_class: problem
    lambda: |-
      // True when the radiator is heating in a mode that will NOT auto-off, i.e.
      // it will stay on until something turns it off. Observed on this firmware:
      //   0x00 / 0x20 = off,  0x09 = physical timer (bounded, auto-off, red light),
      //   0x21 = manual / HA-driven continuous heating (no auto-off).
      // Anything on that is not the known timer value is treated as "no timer" so
      // we err toward alerting. Add other timer values here if you find them.
      int m = id(operating_mode);
      bool off = (m == 0x00 || m == 0x20);
      bool timer = (m == 0x09);
      return (!off && !timer);

# ============================================
# Rail target temperature (native element regulation)
# ============================================
# The radiator itself holds the element at this temperature in mode 6. Changing
# this while the radiator is ON pushes the new target to the device immediately.
number:
  - platform: template
    name: "Rail Target Temperature"
    id: rail_target
    icon: "mdi:thermometer"
    unit_of_measurement: "°C"
    min_value: 30
    max_value: 60
    # 5 discrete levels matching the radiator's physical buttons: 30 / 37.5 / 45 / 52.5 / 60
    step: 7.5
    initial_value: 45
    restore_value: true
    optimistic: true
    set_action:
      - if:
          condition:
            lambda: "return id(is_connected) && id(power_state);"
          then:
            - ble_client.ble_write:
                id: terma_moa_ble_client
                service_uuid: "d97352b0-d19e-11e2-9e96-0800200c9a66"
                characteristic_uuid: "d97352b2-d19e-11e2-9e96-0800200c9a66"
                value: !lambda |-
                  // Element target = little-endian uint16 (°C * 10) in bytes 2-3.
                  // Use x (the just-set value): the number's .state is not updated
                  // until AFTER set_action runs, so reading .state here lags by one.
                  uint16_t v = (uint16_t)(x * 10.0f);
                  return {0x00, 0x00, (uint8_t)(v & 0xFF), (uint8_t)(v >> 8)};
            - logger.log:
                format: "Rail target updated to %.1f°C (pushed to radiator)"
                args: ["x"]

# Switches - radiator on/off (radiator self-regulates the element in mode 6)
switch:
  - platform: template
    name: "Radiator Power"
    id: power_switch
    icon: "mdi:power"
    optimistic: false
    lambda: |-
      return id(power_state);
    turn_on_action:
      - if:
          condition:
            lambda: "return id(is_connected);"
          then:
            - logger.log: "Radiator ON (manual/continuous 0x21, target from Rail Target)"
            - lambda: |-
                id(power_state) = true;
                id(operating_mode) = 0x21;
            - ble_client.ble_write:
                id: terma_moa_ble_client
                service_uuid: "d97352b0-d19e-11e2-9e96-0800200c9a66"
                characteristic_uuid: "d97352b2-d19e-11e2-9e96-0800200c9a66"
                value: !lambda |-
                  // Element target from the Rail Target number: little-endian uint16 (°C * 10).
                  uint16_t v = (uint16_t)(id(rail_target).state * 10.0f);
                  return {0x00, 0x00, (uint8_t)(v & 0xFF), (uint8_t)(v >> 8)};
            - delay: 500ms
            - ble_client.ble_write:
                id: terma_moa_ble_client
                service_uuid: "d97352b0-d19e-11e2-9e96-0800200c9a66"
                characteristic_uuid: "d97352b3-d19e-11e2-9e96-0800200c9a66"
                # 0x21 (33) = manual / continuous ON for this firmware (mode 6 is
                # not accepted - the radiator just flickers and reverts to off).
                value: [33, 0, 0, 0]
          else:
            - logger.log: "Not connected - cannot turn on"
    turn_off_action:
      - if:
          condition:
            lambda: "return id(is_connected);"
          then:
            - logger.log: "Radiator OFF"
            - lambda: |-
                id(power_state) = false;
                id(operating_mode) = 0;
            - ble_client.ble_write:
                id: terma_moa_ble_client
                service_uuid: "d97352b0-d19e-11e2-9e96-0800200c9a66"
                characteristic_uuid: "d97352b3-d19e-11e2-9e96-0800200c9a66"
                value: [0, 0, 0, 0]
          else:
            - logger.log: "Not connected - cannot turn off"

# Buttons
button:
  # Start the radiator's bounded auto-off timer (mode 0x09 - the same value the
  # physical timer button produces: red light, auto-off). Heats at the Rail Target
  # temperature. NOTE: writing 0x09 is best-effort (mirrors the observed state);
  # verify on-device that it actually engages the timer and auto-offs. The countdown
  # duration is set by the radiator, not by us.
  - platform: template
    name: "Start Timer"
    icon: "mdi:timer"
    on_press:
      - if:
          condition:
            lambda: "return id(is_connected);"
          then:
            - logger.log: "Starting timer mode (0x09)"
            - lambda: |-
                id(power_state) = true;
                id(operating_mode) = 0x09;
            - ble_client.ble_write:
                id: terma_moa_ble_client
                service_uuid: "d97352b0-d19e-11e2-9e96-0800200c9a66"
                characteristic_uuid: "d97352b2-d19e-11e2-9e96-0800200c9a66"
                value: !lambda |-
                  // Element target from the Rail Target number: little-endian uint16 (°C * 10).
                  uint16_t v = (uint16_t)(id(rail_target).state * 10.0f);
                  return {0x00, 0x00, (uint8_t)(v & 0xFF), (uint8_t)(v >> 8)};
            - delay: 500ms
            - ble_client.ble_write:
                id: terma_moa_ble_client
                service_uuid: "d97352b0-d19e-11e2-9e96-0800200c9a66"
                characteristic_uuid: "d97352b3-d19e-11e2-9e96-0800200c9a66"
                # 0x09 (9) = timer mode observed when the physical timer button is pressed.
                value: [9, 0, 0, 0]
          else:
            - logger.log: "Not connected - cannot start timer"

  - platform: template
    name: "Clear BLE Bonds"
    icon: "mdi:bluetooth-off"
    entity_category: diagnostic
    on_press:
      - logger.log: "Clearing BLE bond for the radiator..."
      # Native replacement for the old raw esp_ble_remove_bond_device lambda.
      # Removes the bond for this radiator (sufficient for a single-device setup).
      - ble_client.remove_bond:
          id: terma_moa_ble_client
      - logger.log: "BLE bond cleared - restart ESP and put radiator in pairing mode"

  - platform: restart
    name: "Restart"
    entity_category: diagnostic

# Text sensor for status
text_sensor:
  - platform: template
    name: "Radiator Status"
    icon: "mdi:information"
    lambda: |-
      if (!id(is_connected)) {
        return {"Disconnected"};
      }
      int m = id(operating_mode);
      switch (m) {
        case 0x00:
        case 0x20:
          return {"Standby"};
        case 0x09:  // physical timer button: bounded, auto-off (red light)
          return {"Timer - Heating (auto-off)"};
        case 0x21:  // manual / HA-driven: continuous, no auto-off
          return {"Manual - Heating (no timer)"};
        // Legacy / older-firmware values, kept for safety:
        case 5: return {"Manual Room - Heating"};
        case 6: return {"Manual Element - Heating"};
        case 7: return {"Schedule Room - Heating"};
        case 8: return {"Schedule Element - Heating"};
        default: {
          char buf[24];
          snprintf(buf, sizeof(buf), "On - mode 0x%02X", m);
          return {std::string(buf)};
        }
      }
    update_interval: 5s

  - platform: wifi_info
    ip_address:
      name: "IP Address"
      entity_category: diagnostic
    ssid:
      name: "Connected SSID"
      entity_category: diagnostic
    mac_address:
      name: "MAC Address"
      entity_category: diagnostic

  - platform: version
    name: "ESPHome Version"
    entity_category: diagnostic
